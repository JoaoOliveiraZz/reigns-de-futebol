import "@/src/styles/global.css";
import { Text, View } from "react-native";

export default function Index() {
  return (
    <View className="h-screen bg-slate-600 flex items-center justify-center">
      <Text className="uppercase font-bold text-slate-300">Edit app/index.tsx to edit this screen.</Text>
    </View>
  );
}
